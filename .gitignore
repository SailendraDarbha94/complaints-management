node_modules/
dist/
.next/
out/
build/
coverage/
*.tsbuildinfo
.turbo/

.env
.env.*
!.env.example

.DS_Store
Thumbs.db
*.log
npm-debug.log*
.pnpm-debug.log*

# Never commit council authorisation artefacts or real case data
docs/authorisation/*
!docs/authorisation/README.md
/tmp/
/scratch/
*.dump
*.sql.gz

# embedded Postgres test data
packages/db/.pgdata-test/
packages/db/.pgdata-dev/
apps/web/.next/
apps/api/.pgdata-test/
next-env.d.ts
# Locally stored documents and the development mail outbox, wherever they land. This
# rule was once apps/api/var/ - when that package moved to packages/core the rule stopped
# matching and ~280 test blobs quietly became stageable. Match the directory, not a path.
var/
.tmp-*
